# Шаг 6.1 — Аналитика структуры директорий проекта

**Дата:** 2025-07-15
**Контекст:** Начало реализации компонентов. Принято решение о структуре директорий.

---

## 🎯 Цель этапа

Формализовать первое инженерное решение по структуре директорий проекта до начала реализации. Цель: обеспечить соответствие организационной структуры исходного кода архитектурной модели, зафиксированной на Шагах 3 (декомпозиция ответственности) и 4 (архитектурное моделирование).

---

## 🔍 От чего отталкивались

1. **Архитектурная модель проекта** (см. `design/decisions/04_architecture.md`) подразумевает слоистую структуру, где каждый слой (абстракции, координаторы, исполнители, тесты) изолирован логически и концептуально.

2. В [шаге 3](./03_layers.md) мы уже зафиксировали, что в проекте существуют следующие слои:

   * Входной слой (Program)
   * Координаторы (Sender)
   * Интерфейсы (IMessenger)
   * Исполнители (реализации IMessenger)
   * Тесты как отдельный инженерный механизм

3. Проект учебный, с прицелом на **воспроизводимость архитектурных решений и масштабируемость инженерных практик**. Это требует явной трассируемости между уровнями проектирования и файлами исходного кода.

---

## 🧠 Принятые решения (аналитика)

1. **Выделение отдельных директорий под каждый логический слой.**
   Это облегчает навигацию, поддержку SRP и соответствует идее архитектурной изоляции. Были созданы:

   * `src/Coordiantors/`
   * `src/Interfaces/`
   * `src/Performers/`
   * `tests/`

2. **Оставление Program.cs в корне проекта.**
   Это типовой стандарт .NET-проектов, подчёркивает роль Program.cs как точки входа и делает структуру интуитивно понятной без переопределения общепринятых соглашений.

3. **Интерфейсы остаются обособленными.**
   Несмотря на возможность переноса `IMessenger` в директорию исполнителей, решено оставить его в `Interfaces/`, так как он отражает **абстрактный контракт**, лежащий выше конкретных реализаций. Это подчёркивает архитектурную роль и обеспечивает слабую связность.

4. **Тесты выделены в корень как отдельная папка `tests/`.**
   Это сделано по следующим причинам:

   * Позволяет структурировать тесты независимо от реализации (своя логика группировки — по координаторам и исполнителям);
   * Не привязывает тесты физически к слоям реализации;
   * Подчёркивает статус тестов как инструмента верификации требований, а не как части приложения.

---

## 📌 Заключение

Структура директорий напрямую следует из архитектурных решений, зафиксированных в проектной документации. Она подчёркивает изоляцию компонентов, поддерживает слабую связность и соответствует слоистой модели проектирования. Данное разделение станет основой для реализации компонентов в рамках следующего этапа.
